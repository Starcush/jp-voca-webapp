# jp-voca-webapp

Japanese vocabulary notebook web app MVP.

## Getting started

```bash
nvm use
npm install
npm run dev
```

Copy `.env.local.example` to `.env.local` and fill in the Firebase values before using Firestore-backed features.

