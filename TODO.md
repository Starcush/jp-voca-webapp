# TODO

이 문서는 프로젝트의 기능 작업과 UI/UX 개선 사항을 하나로 관리하는 작업 목록입니다.

## 우선순위 기준

- `P0`: 지금 사용성이나 핵심 플로우를 막는 작업. 다음 작업 후보로 가장 먼저 본다.
- `P1`: MVP 검증과 차별화에 필요한 작업. P0 이후 바로 이어서 본다.
- `P2`: 1차 검증 후 있으면 확실히 좋아지는 확장 작업.
- `P3`: 장기 검토 또는 지금은 하지 않아도 되는 작업.

## 다음 작업 후보

1. `[P0][UI/UX]` 데스크탑 UI 개선
   - 사용자가 데스크탑에서 자주 사용할 예정이므로 모바일 중심 레이아웃을 그대로 늘린 현재 화면은 불편하다.
   - 목록, 가져오기, 정리, 복습의 데스크탑 레이아웃을 먼저 점검한다.
   - 데스크탑에서는 하단 탭보다 좌측 내비게이션, 넓은 목록/상세 분할, 더 조밀한 테이블형 보기를 검토한다.
2. `[P0][UI/UX]` OCR 저장 전 확인 목록 UX 개선
   - 50개까지 담을 수 있어 목록이 길어졌고, 읽기/뜻 누락 항목을 찾기 어렵다.
   - `저장 준비 N개 / 확인 필요 M개` 요약과 `전체 / 확인 필요` 필터를 검토한다.
   - 저장 액션이 긴 목록 아래에 묻히지 않도록 sticky 요약/액션 영역을 검토한다.
3. `[P1][기능]` 복습 세션 UX 개선
   - 한글 뜻을 보고 원어 단어를 떠올리는 역방향 복습 모드를 검토한다.
   - 노트별 복습과 헷갈림 표시 단어 복습은 Anki/FSRS 도입 후 다시 검토한다.

## 기능 작업

### OCR / 가져오기

- [x] `[P0]` OCR 읽기/뜻 채우기를 10개씩 나눠 순차 처리한다.
  - 전체 추가 예정 표현은 최대 50개까지 유지한다.
  - API 요청은 내부적으로 10개 단위로 처리한다.
  - 진행률을 표시한다. 예: `20 / 37개 완료`.
  - 중간 실패 시 가능한 결과는 반영하고 사용자에게 안내한다.
- [x] `[P0]` OCR 진행 중 UI를 개선한다.
  - 오버레이 문구를 진행률 포함 형태로 바꾼다.
- [x] `[P0]` 일본어 세로쓰기 OCR 읽기 순서 문제를 개선한다.
  - 일본어 OCR 화면에 `자동 / 가로쓰기 / 세로쓰기` 읽기 방향 선택을 추가했다.
  - 세로쓰기 모드에서는 Google Vision word bounding box를 기준으로 오른쪽 열 -> 왼쪽 열 순서로 재정렬한다.
  - 실제 문제가 발견됐던 책 사진으로 본문 읽기 순서를 테스트했다.
- [ ] `[P1]` OCR 문장 분리 결과를 사용자가 수정할 수 있게 한다.
  - 추출된 텍스트 수정 후 다시 문장 분리.
  - 나중에 문장 합치기/나누기까지 확장한다.
- [ ] `[P1]` 일본어 세로쓰기 OCR 자동 판별과 잡음 제거를 더 다듬는다.
  - 페이지 번호, 문제 번호, 장식성 텍스트, `0` 같은 잡음 제거 휴리스틱을 실제 사진 샘플로 조정한다.
  - 현재 읽기 방향 옵션은 테스트를 위해 노출되어 있지만, 나중에는 고급 옵션이나 `세로쓰기 다시 시도`로 낮출지 검토한다.
- [ ] `[P1]` 일본어 표현 분석 API를 별도로 만들지 결정한다.
  - 후보: `/api/analyze-expression`.
  - `/api/meaning`과 역할을 분리할지 검토한다.

### 노트와 단어 관리

- [x] `[P0]` 노트 데이터 모델과 Firestore Rules를 추가한다.
- [x] `[P0]` 노트 관리 화면을 만든다.
  - 노트 생성, 이름 수정, 삭제를 정리 탭 하위 화면에서 처리한다.
- [x] `[P0]` 단어 관리 화면을 만든다.
  - 여러 단어 이동/삭제를 정리 탭 하위 화면에서 처리한다.
  - 이동/삭제 전 확인 다이얼로그를 거친다.
- [x] `[P0]` 목록과 가져오기에서 공통 노트 드롭다운을 사용한다.
- [x] `[P0]` 노트 안에서 단어 추가/OCR 추가로 들어가는 흐름을 더 명확히 한다.
  - 목록 플로팅 액션에서 현재 노트를 유지한 채 `사진`/`추가`로 진입하게 했다.
  - 단어 추가 화면에서도 OCR과 같은 공통 저장 노트 드롭다운으로 노트를 바꿀 수 있게 했다.
- [ ] `[P0]` 단어 추가/OCR 화면의 저장 노트 안내를 배포 화면에서 확인한다.
- [ ] `[P2]` 삭제되었거나 찾을 수 없는 노트 접근 시 UX를 더 다듬는다.
- [ ] `[P1]` 노트별 단어 수, 복습 수, 최근 추가일 표시를 검토한다.

### 복습

- [x] `[P0]` 복습 페이지 MVP를 만든다.
- [x] `[P1]` 복습 화면 1차 디자인을 앱 톤에 맞춘다.
  - 복습 카드, 진행률, 판정 버튼, 완료/빈/로딩/에러 상태를 정리했다.
- [x] `[P1]` 복습 모드 필터를 제거한다.
  - `추천`, `모르는 단어`, `오래 안 본 단어`, `전체 섞기` 탭을 제거했다.
  - 내부 정렬은 기존 추천 기준을 기본값으로 유지한다.
- [ ] `[P1]` 한글 뜻 기준으로 원어 단어를 떠올리는 복습 모드를 추가한다.
- [ ] `[P1]` Anki FSRS 또는 `ts-fsrs` 기반 복습 스케줄링 도입을 검토한다.
  - 단순히 오래 안 본 단어가 아니라 복습 우선순위를 계산하는 방향.
  - 기존 `알았어요` / `모르겠어요` 판정 데이터를 어떻게 연결할지 설계한다.
- [ ] `[P2]` AI 힌트 카드 실험을 검토한다.
  - 단어 의미를 선 중심 일러스트나 간단한 힌트로 보여주는 아이디어.
  - 이미지 생성은 비용이 생길 수 있으므로 단어별 1회 생성 후 캐시하는 방식이 필요하다.

### 시트 보기

- [ ] `[P1]` 앱 안에 스프레드시트형 학습 보기 화면을 기획한다.
  - 카드형은 학습 집중용.
  - 시트형은 아이패드/데스크탑에서 많이 훑어보는 용도.
- [ ] `[P1]` 시트 보기 MVP 컬럼을 정한다.
  - 일본어: 단어, 읽기, 뜻, 예문.
  - 중국어: 단어, pinyin, 뜻, 예문.
  - 영어: 단어, 뜻, 예문.
- [ ] `[P2]` 시트 보기에서 제공할 학습 옵션을 정한다.
  - 뜻 가리기, 읽기 가리기, 예문 표시/숨김, 노트별 필터, 검색.
- [ ] `[P3]` 나중에 CSV/Excel export를 검토한다.

### 문법 / 표현 카드

- [ ] `[P1]` 일본어 표현 분석 기능을 기획한다.
  - 선택 표현의 원형을 알려준다.
  - 예: `食べて` -> `食べる`, `て형`.
  - 품사, 활용, 간단한 문법 설명을 표시한다.
- [ ] `[P1]` 문법/표현 카드 MVP 데이터 모델을 정한다.
  - 문법 패턴, 의미, 접속 방식, 원문 예문, 해석, 헷갈리는 표현, 태그.
  - OCR로 캡처한 원문 자체를 실사용 예문으로 저장하는 흐름을 우선한다.
- [ ] `[P2]` 한국어 의미 검색으로 문법/표현 카드가 검색되는 방식을 검토한다.

### 회원가입과 계정

- [x] `[P0]` 설정 페이지를 추가한다.
  - 계정 관리, 언어 관리 메뉴를 분리한다.
  - 로그아웃과 회원탈퇴 안내를 제공한다.
- [ ] `[P3]` 소셜 로그인 도입을 기획한다.
  - Google 로그인, Apple 로그인.
  - 기존 이메일 계정과 소셜 계정 연결.
- [ ] `[P3]` 같은 이메일의 로그인 제공자가 다를 때 계정이 갈라지지 않도록 설계한다.

### 크레딧과 수익화

- [ ] `[P1]` 사용량 기록 모델을 먼저 설계한다.
  - OCR 요청 수, AI 읽기/뜻 요청 수, 표현 분석 요청 수, AI 대화/퀴즈 요청 수.
  - `feature_type` 같은 필드로 기능별 사용량을 남긴다.
- [ ] `[P1]` 크레딧 모델을 설계한다.
  - 무료 월 기본 크레딧.
  - 관리자/개발자/가족 계정 무제한 또는 높은 한도.
  - 실제 결제 전에는 내부 차감 로직부터 만든다.
- [ ] `[P2]` 가격 정책은 실제 사용량 데이터를 보고 정한다.

### 어드민

- [ ] `[P3]` 어드민 MVP 범위를 정한다.
  - 유저 목록, 유저 상세, 로그인 방식, 크레딧 잔액, 사용량 기록, 크레딧 수동 지급/차감, 유저 상태 변경, 관리자 메모.
- [ ] `[P3]` 어드민 권한 모델을 설계한다.
  - 클라이언트 체크만으로 막지 않는다.
  - 서버/API/Firestore Rules에서 보호한다.
  - 어드민 액션은 audit log를 남긴다.
- [ ] `[P3]` 결제 도입 후 환불/결제 내역 관리를 추가한다.

## UI/UX 작업

### 완료된 개선

- [x] `[P0]` 목록 화면 보기/필터 버튼 의미를 정리한다.
  - 처리 커밋: `5359250 ui: 단어 목록 보기 토글 정리`
  - 상태 필터 `"전체"`, `"모르는 것만"`, `"오래 안 본 것"`을 제거했다.
  - 카드 표시 토글은 `"전체"`, `"한자만 보기"`, `"뜻만 보기"`로 정리했다.
- [x] `[P0]` 단어 카드의 체크/물음표 버튼 역할을 정리한다.
  - 처리 커밋: `d947858 ui: 단어 목록 헷갈림 표시 정리`
  - 목록의 `알았어요` / `모르겠어요` 버튼을 제거했다.
  - 행 클릭으로 임시 `헷갈림 표시`를 켜고 끌 수 있게 했다.
- [x] `[P0]` 목록 화면의 선택/삭제 기능을 정리 탭으로 이동한다.
  - 처리 커밋: `358d65a ui: 단어 관리 화면 정리`
  - 기본 목록은 학습/조회 중심, 정리 탭은 관리 작업 중심으로 역할을 나눴다.
- [x] `[P0]` 정리 탭 정보 구조를 정리한다.
  - 처리 커밋: `d96e4d4 ui: 정리 탭 메뉴 구조 추가`
  - 정리 탭을 메뉴 랜딩으로 바꾸고 `노트 관리`, `단어 관리` 하위 화면으로 분리했다.
- [x] `[P0]` 설정 페이지 진입 방식을 정리한다.
  - 처리 커밋: `02abae0 ui: 설정 메뉴 구조화`
  - 하단 탭은 학습 기능 4개로 유지하고, 설정은 상단 아이콘으로 진입한다.
- [x] `[P0]` 목록/가져오기 하단 여백과 노트 드롭다운을 정리한다.
  - 처리 커밋: `aaf3f97 ui: 노트 드롭다운 공통화`
  - 처리 커밋: `767a444 ui: 화면 하단 여백 정리`

### 진행 중 / 다음 개선

- [ ] `[P0]` 데스크탑 UI를 개선한다.
  - 현재 화면은 모바일 기준 구조가 데스크탑에 그대로 늘어난 느낌이라 사용성이 떨어진다.
  - 데스크탑 목록은 더 넓은 공간을 활용해 단어 확인, 검색, 노트 선택, 카드/시트 보기를 재배치한다.
  - 하단 탭을 데스크탑에서도 유지할지, 좌측 내비게이션으로 바꿀지 검토한다.
- [ ] `[P0]` OCR 가져오기 화면의 긴 목록 검토 UX를 개선한다.
  - 처리 커밋: `11e2cec ui: OCR 추가 예정 목록 액션 정리`
  - 처리 커밋: `cc7b287 ui: 가져오기 저장 노트 선택 개선`
  - 읽기/뜻을 채우지 않은 상태로 저장할 때 확인은 거치지만, 누락 항목을 빠르게 찾는 UX가 아직 부족하다.
- [ ] `[P0]` OCR 버튼 라벨을 다시 정리한다.
  - `의미 찾기` 후보: `읽기·뜻 채우기`.
  - `추가 예정` 후보: `저장 전 확인`.
  - `문장별 선택` 후보: `문장에서 표현 고르기`.
- [x] `[P0]` 탭 전환 시 화면이 깨지는 문제를 수정한다.
  - 라우트 전환 중 `AppNavigation`이 잠깐 비면서 본문이 데스크탑 grid의 첫 번째 좁은 컬럼에 들어가는 문제를 수정했다.
  - `AppNavigationFallback`을 추가해 전환 중에도 좌측 메뉴/하단 탭의 공간을 유지한다.
  - 본문 영역은 데스크탑에서 항상 두 번째 컬럼에 고정되도록 했다.
  - 탭 클릭 시 네비게이션은 유지하고 콘텐츠 영역만 블러/로딩 오버레이로 전환 상태를 보여준다.
- [ ] `[P1]` 노트 개념을 더 가볍게 이해시키는 UX를 검토한다.
  - `전체`, `미분류`, 개별 노트의 의미가 첫 사용자에게 부담스럽지 않아야 한다.
  - 노트는 "필요하면 쓰는 단어장 묶음"처럼 설명하는 방향을 검토한다.
- [ ] `[P1]` 일본어 세로쓰기 OCR 설정 UX를 더 다듬는다.
  - 현재 `자동 / 가로쓰기 / 세로쓰기`가 항상 노출되어 초보자에게 어려워 보일 수 있다.
  - 자동 판별이 안정되면 수동 설정은 고급 옵션으로 낮춘다.

## 디자인 외주 전 준비

- [ ] `[P1]` 디자이너에게 보여줄 핵심 화면 목록을 정리한다.
  - 로그인/가입.
  - 언어 선택.
  - 단어 목록.
  - 노트 관리.
  - 단어 추가.
  - OCR 사진 업로드.
  - OCR 문장 선택.
  - OCR 저장 전 확인.
  - 복습.
  - 시트 보기.
  - 단어 정리/노트 이동.
- [ ] `[P1]` 제품 핵심 플로우를 한 문장으로 정리한다.
  - 사진 찍기 -> 텍스트 추출 -> 표현 선택 -> 읽기/뜻 채우기 -> 저장 -> 시트로 보기 -> 복습.
- [ ] `[P1]` 카드형 보기, 시트형 보기, 복습 모드, 정리 모드의 역할을 구분한다.

## 지금 하지 않아도 되는 일

- [ ] `[P3]` 결제 본구현.
- [ ] `[P3]` 어드민 전체 구현.
- [ ] `[P3]` AI 대화 학습 본구현.
- [ ] `[P3]` 완전한 문법 자동 분석.
- [ ] `[P3]` Google Sheets 직접 연동.
- [ ] `[P3]` 고급 통계 대시보드.
- [ ] `[P3]` 랜딩 페이지 완성.
- [ ] `[P3]` 브랜딩 전체 작업.
