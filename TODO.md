# TODO

이 문서는 프로젝트에 넣고 싶은 큰 기능을 중요도에 따라 관리합니다.
하이라이트 색상, 버튼 라벨, API 분리처럼 하나의 기능 안에서 필요한 일은 독립 우선순위로 만들지 않고 해당 기능의 세부 작업으로 기록합니다.

## 제품 정체성

이 앱은 모르는 단어를 한 번 검색하고 저장하는 단어장에 머무르지 않습니다.
공부하면서 만난 단어, 문장, 문법, 표현과 출처가 계속 쌓이고, 사용자가 그 학습 데이터를 다시 찾고, 이해하고, 정리하고, 복습하고, 실제 언어 사용으로 연결하게 만드는 도구를 지향합니다.

핵심 흐름:

```text
발견과 수집
OCR · 직접 입력 · import
        ↓
이해와 연결
읽기 · 뜻 · 형태 분석 · 문법/표현 · 원문 문장
        ↓
축적과 관리
노트 · 출처 · 검색 · 중복 정리 · 보관
        ↓
다시 활용
FSRS 복습 · 상황극 프롬프트 · 공유 · export
```

제품 원칙:

- 단어가 적을 때뿐 아니라 수백·수천 개로 쌓였을 때도 사용할 수 있어야 한다.
- 저장 자체보다 저장된 학습 데이터가 시간이 지나도 다시 쓰이는 것을 중요하게 본다.
- 단어, 문장, 형태 분석, 문법·표현 카드는 서로 고립되지 않고 원문과 출처를 중심으로 연결될 수 있어야 한다.
- 사용자가 모든 자료를 직접 정리하도록 강요하지 않고 검색, 추천, 중복 감지, 보관 같은 방식으로 정리를 돕는다.
- 새로운 기능을 기획할 때 `더 쉽게 수집하는가`, `더 잘 이해하는가`, `쌓인 자료를 관리하는가`, `다시 활용하게 하는가`를 함께 확인한다.
- 구체적인 관리 방식은 미리 확정하지 않고 실제로 데이터가 쌓이는 과정과 사용자 행동을 보며 발전시킨다.

## 우선순위 기준

- `A · 필수`: 이 앱에 반드시 넣고 싶은 핵심 기능.
- `B · 권장`: 핵심 기능이 안정된 뒤 제품 가치를 높이기 위해 넣고 싶은 기능.
- `C · 선택`: 사용자 반응이나 운영 필요가 확인되면 진행할 기능.

표기 원칙:

- A, B, C는 중요도를 나타낼 뿐 실제 작업 순서를 의미하지 않는다.
- 같은 등급 안의 항목 배치도 작업 순서가 아니다.
- 실제 다음 작업은 당시 사용성 문제, 기술 의존성, 비용 위험, 사용자 피드백을 보고 별도로 정한다.
- 우선순위와 기능 범위는 대화와 사용 경험에 따라 계속 수정한다.

## A · 필수

### OCR 개선

- [ ] 일본어, 영어, 중국어에서 사진 속 표현을 쉽게 골라 저장하는 전체 경험을 완성한다.

현재 상태:

- Google Vision으로 사진의 텍스트를 추출할 수 있다.
- 일본어 가로쓰기와 세로쓰기 순서를 보정할 수 있다.
- 기존 문장 기반 가져오기와 별도의 사진 위 선택 테스트 화면이 있다.
- 최대 50개 표현을 담고, 읽기와 뜻은 내부적으로 10개씩 처리한다.

세부 작업:

- 사진 위에서 인식 가능한 영역과 사용자가 선택한 영역을 명확히 구분한다.
- primary 보색인 sky blue를 OCR 선택 색상으로 검증한다.
- Android Chrome, iOS Safari, 데스크탑 브라우저에서 드래그, 확대, 이동을 확인한다.
- 일본어, 영어, 중국어 모두 같은 선택 흐름을 제공하되 읽기와 분석 필드는 언어별로 다르게 처리한다.
- 세로쓰기 자동 판별, 페이지 번호와 장식 문자 같은 OCR 잡음 제거를 개선한다.
- 문장 수정, 합치기, 나누기는 OCR 결과가 이상할 때 사용하는 보조 기능으로 둔다.
- 저장 전 확인에서 읽기나 뜻을 찾지 못한 표현을 빠르게 확인할 수 있게 한다.
- 기존 문장 기반 방식과 사진 위 선택 방식은 실제 사용자 테스트 후 통합 여부를 결정한다.

완료 기준:

- 처음 사용하는 사람이 설명 없이 사진 선택부터 단어 저장까지 완료할 수 있다.
- 일본어, 영어, 중국어에서 동일한 핵심 흐름이 동작한다.
- 모바일에서 선택 동작 때문에 저장을 포기하는 문제가 없어야 한다.

### AI 사용량 기록과 무료 크레딧 제한

- [ ] 비용이 발생하는 기능을 여러 사용자에게 열어도 운영 가능한 최소 구조를 만든다.

세부 작업:

- OCR, 읽기·뜻 추천, 형태 분석처럼 비용이 발생하는 요청을 기능별로 기록한다.
- 사용 로그에 `feature_type`, 사용자, 사용량, 발생 시각을 저장한다.
- 사용자에게 매월 무료 크레딧을 지급하고 소진 시 비용 기능 사용을 제한한다.
- 개발자, 가족, 테스트 계정은 간단한 화이트리스트로 예외 처리한다.
- 기본 단어 저장, 노트, 복습, import/export는 무료로 유지한다.
- 실제 결제와 가격은 사용량 데이터가 쌓인 뒤 결정한다.

완료 기준:

- 기능별 사용량과 예상 비용을 확인할 수 있다.
- 무료 한도를 넘긴 요청이 서버에서 차단된다.
- 관리자 UI 없이도 DB 설정으로 테스트 계정을 관리할 수 있다.

### 상황극 프롬프트 생성

- [ ] 저장한 단어를 실제 회화에서 사용하도록 외부 ChatGPT용 프롬프트를 만든다.

세부 작업:

- 일본어, 영어, 중국어 모드를 모두 제공한다.
- 노트 전체 또는 사용자가 고른 여러 단어를 입력으로 받는다.
- 선택한 모든 단어를 최소 1회 이상 자연스럽게 사용하도록 지시한다.
- ChatGPT가 사용한 단어와 아직 사용하지 않은 단어를 추적하도록 한다.
- 단어가 많으면 ChatGPT가 의미 단위로 나눠 순차적으로 진행하도록 한다.
- 앱에서 AI를 직접 호출하지 않고 서버에서 프롬프트 텍스트만 조립한다.
- 생성한 프롬프트는 클립보드에 복사해 외부 ChatGPT에서 사용한다.

언어별 방향:

- 일본어: 자연스러운 원어민 표현과 상황별 말투를 강조한다.
- 영어: 실제 회화와 문맥에 맞는 표현 사용을 강조한다.
- 중국어: 간체 문장, pinyin 보조 여부, 자연스러운 구어 표현을 고려한다.

완료 기준:

- 사용자가 노트나 단어를 고르고 한 번의 흐름으로 프롬프트를 복사할 수 있다.
- 생성된 대화에서 선택한 모든 단어의 사용 여부를 확인할 수 있다.

### 단어 상세와 언어별 분석

- [ ] 저장된 단어나 표현이 문장에서 어떤 형태와 의미로 사용됐는지 이해할 수 있는 상세 화면을 만든다.

공통 정보:

- 입력된 표면형
- 대표형 또는 원형
- 읽기 또는 발음 정보
- 품사와 문맥 의미
- 구성 요소와 결합 표현
- 원문 예문과 해석
- AI 분석 결과 수정 기능

언어별 방향:

- 일본어: `味わって -> 味わう / て형`, `励まし合う -> 励ます + 合う`처럼 활용과 복합 표현을 분석한다.
- 영어: 원형, 시제와 분사, 품사, phrasal verb와 idiom을 분석한다.
- 중국어: 단어 분리, pinyin, 간체·번체, 문장 안에서의 품사와 결합 표현을 분석한다.

구현 방향:

- `/api/meaning`은 짧은 읽기·뜻 추천 역할을 유지한다.
- `/api/analyze-expression`은 원형, 활용, 구성 요소, 문맥 의미를 구조화해 반환한다.
- AI 결과임을 명확히 표시하고 사용자가 수정할 수 있게 한다.

완료 기준:

- 목록에서 단어 상세 화면으로 이동할 수 있다.
- 언어별로 필요한 분석 정보가 다르게 표시된다.
- 단순 활용과 복합 표현을 구분해 설명할 수 있다.

### 사용자 수준과 학습 상태를 반영한 맞춤형 예문 생성

- [ ] 사용자의 언어 수준과 단어 학습 상태를 조합해 이해 가능한 맞춤형 예문을 생성한다.

사용자 수준 설정:

- 처음 가입하거나 학습 언어를 추가할 때 선택적으로 언어 수준을 입력받는다.
- 일본어는 JLPT 등급과 JPT 점수, 영어는 CEFR과 TOEIC, 중국어는 HSK를 우선 검토한다.
- 수준을 모르거나 입력하고 싶지 않은 사용자는 건너뛸 수 있다.
- 입력한 수준은 설정에서 언제든지 수정할 수 있게 한다.
- 최초 입력값은 절대적인 실력 판정이 아니라 예문 난이도를 정하는 초기 참고값으로 사용한다.

학습 데이터 활용:

- 사용자가 안다고 표시한 단어와 복습에서 안정적으로 맞힌 단어를 익숙한 어휘로 본다.
- 사용자가 모른다고 표시한 단어, 최근 저장한 단어와 자주 틀린 단어를 학습 중인 어휘로 본다.
- 생성할 때는 익숙한 어휘 약 70~80%, 학습 중인 어휘 약 20~30%를 목표 범위로 사용한다.
- 조사와 조동사 같은 기능어를 제외하고 의미가 있는 어휘를 중심으로 비율을 계산한다.
- 단어장 전체를 AI에 전달하지 않고 서버에서 현재 대상과 관련된 소수의 단어만 선별한다.

예문 생성 흐름:

- 단어 상세 화면에서 `내 수준에 맞는 예문 생성` 기능을 제공한다.
- 연습할 대상 단어를 반드시 포함하고, 사용자 수준에 맞춰 문장 길이와 문법 난이도를 조절한다.
- AI API에는 대상 단어, 선별한 익숙한 단어와 학습 중인 단어, 언어 수준과 출력 규칙을 구조화해 전달한다.
- 예문, 해석, 읽기, 사용한 학습 단어와 짧은 설명을 구조화된 결과로 받는다.
- 생성된 문장을 형태 분석해 목표 어휘 비율과 대상 단어 포함 여부를 검증한다.
- 목표 범위를 크게 벗어난 경우 재생성하거나 사용자가 `더 쉽게`, `비슷하게`, `더 어렵게` 조절할 수 있게 한다.
- 책이나 사진에서 가져온 원문 예문과 AI가 생성한 예문을 데이터와 UI에서 명확히 구분한다.
- 생성 결과를 저장하고 재사용해 API 비용을 줄이며, 재생성은 사용량 또는 크레딧에 반영한다.

첫 MVP:

- 일본어와 단어 상세 화면부터 시작한다.
- 대상 단어 1개와 사용자가 아는 단어 3~8개를 이용해 예문 1개를 생성한다.
- JLPT 수준에 따라 문장 길이와 문법 난이도를 조절한다.
- 예문, 해석과 읽기를 JSON으로 받고 `더 쉽게`, `다시 생성`을 제공한다.

완료 기준:

- 사용자가 자신의 수준과 학습 중인 단어를 반영한 예문을 단어 상세에서 생성할 수 있다.
- 생성된 예문이 대상 단어를 포함하고 설정한 난이도와 어휘 비율에 대체로 맞는다.
- 원문 예문과 AI 생성 예문을 구분해 저장하고 다시 확인할 수 있다.

## B · 권장

### 읽기 전용 노트 공유 viewer

- [ ] 링크를 받은 사람이 로그인 없이 노트의 단어를 볼 수 있게 한다.

방향:

- 공유 화면에서는 목록과 `전체`, `단어만 보기`, `뜻만 보기` 필터만 제공한다.
- 수정, 삭제, 헷갈림 표시는 제공하지 않는다.
- 추가나 편집이 필요해질 때만 회원가입과 권한 기능을 검토한다.
- `codex/notebook-share-viewer` 브랜치에 초안이 있으며 `main`에는 아직 병합하지 않았다.
- Firebase Admin 프로젝트 설정 불일치 문제를 해결하고 실제 링크를 검증해야 한다.

### 노트 개념 개선

- [ ] 책, 챕터, 수업처럼 사용자가 원하는 단위로 단어를 묶는 경험을 더 쉽게 만든다.

방향:

- 현재는 책이나 챕터마다 하나의 노트를 만드는 방식으로 사용한다.
- `전체`, `미분류`, 개별 노트의 차이를 처음 사용자도 이해할 수 있게 한다.
- 노트별 단어 수, 복습 대상 수, 최근 추가일 표시를 검토한다.
- 상하위 노트나 복잡한 폴더 구조는 실제 필요가 확인되기 전에는 만들지 않는다.

### 문법·표현 카드

- [ ] 문장에서 발견한 문법 패턴과 표현을 단어와 다른 카드로 저장한다.

형태 분석과의 차이:

- 형태 분석은 현재 입력된 표현을 원형, 활용, 구성 요소로 해체한다.
- 문법·표현 카드는 `〜め`, `〜合う`, `だけど`처럼 다른 문장에서도 다시 사용할 학습 개념을 저장한다.
- 형태 분석 결과에서 저장할 만한 패턴을 문법·표현 카드 후보로 제안할 수 있다.

연결 방향:

- 형태 분석과 문법·표현 카드를 완전히 분리된 기능으로 만들지 않는다.
- 형태 분석에서 재사용 가능한 패턴을 발견하면 문법·표현 카드 저장 후보로 제안한다.
- 원문 문장, 저장 단어, 분석 결과, 문법·표현 카드가 서로 연결될 수 있게 한다.
- 예: `励まし合う` 상세에서 `励ます + 合う`를 보여주고, `〜合う` 카드를 열거나 새로 저장할 수 있게 한다.
- 동일한 문법 카드가 여러 단어와 원문 문장에 연결될 수 있는 구조를 검토한다.
- 별도 컬렉션으로 나눌지 하나의 콘텐츠 타입으로 관리할지는 데이터가 쌓이는 모습을 본 뒤 결정한다.

카드 정보:

- 문법 패턴 또는 표현
- 의미와 접속 방식
- OCR에서 가져온 원문 예문과 해석
- 비슷하거나 헷갈리는 표현
- 원어민이 자연스럽게 사용하는 상황에 대한 짧은 코멘트

### 단어장 import/export 포맷

- [ ] 많은 단어를 복사, 붙여넣기, 가져오기, 내보내기 할 수 있는 공통 포맷을 정한다.

방향:

- 앱 안의 시트형 화면 자체보다 데이터 이동과 대량 입력을 우선한다.
- 공통 필드는 언어, 단어, 읽기, 뜻, 예문, 예문 해석, 노트로 구성한다.
- 영어는 읽기 필드를 생략할 수 있고, 일본어와 중국어는 읽기 필드를 사용한다.
- 첫 MVP는 스프레드시트에서 바로 붙여넣을 수 있는 TSV 또는 CSV를 검토한다.
- 같은 포맷으로 내보낸 파일을 다시 가져올 수 있게 한다.
- 중복 단어, 잘못된 열, 필수값 누락을 저장 전에 확인한다.
- Google Sheets 직접 연동은 나중에 검토한다.

### AI 힌트 일러스트

- [ ] 복습 카드에서 단어 의미를 직접 노출하지 않는 간단한 시각 힌트를 제공한다.

방향:

- 선 중심의 단순한 일러스트를 우선 검토한다.
- 이미지 생성 비용 때문에 단어별로 한 번 생성하고 캐시한다.
- 실제 기억에 도움이 되는지 작은 사용자 테스트 후 확대한다.

### 소셜 로그인, 결제, 어드민

- [ ] 사용자와 비용이 늘어날 때 필요한 운영 기능을 단계적으로 만든다.

방향:

- 소셜 로그인은 이메일 가입이 실제 이탈 원인이 될 때 Google과 Apple부터 검토한다.
- 기존 이메일 계정과 소셜 계정을 연결해 계정이 갈라지지 않게 한다.
- 결제는 무료 크레딧과 사용량 기록이 검증된 뒤 도입한다.
- 어드민은 유저, 크레딧, 사용량, 상태, 메모를 관리하는 최소 범위부터 시작한다.
- 결제 이후 환불, 결제 내역, audit log를 추가한다.

## C · 선택

- [ ] OCR 문장 합치기와 나누기의 고급 편집 기능
- [ ] 복습 완료 요약과 고급 학습 통계
- [ ] JLPT 레벨 자동 태깅과 필터
- [ ] 브라우저 확장 프로그램
- [ ] 노트 공동 편집 권한
- [ ] 근대문학 작품 전체 독해 콘텐츠
- [ ] AI 기반 JLPT 청해·독해 문제 생성
- [ ] 외국인 대상 한국어 학습 버전
- [ ] 앱 안에서 직접 대화하는 AI 챗봇
- [ ] 고급 통계 대시보드
- [ ] 랜딩 페이지와 브랜딩 전체 작업

## 단어 데이터 관리 방향성 논의 내용

이 영역은 완료 체크를 위한 작업 목록이 아닙니다.
단어 데이터가 쌓여가는 모습을 보면서 관리 방향에 관한 생각, 대화, 열린 질문을 계속 기록하고 수정합니다.

현재 생각:

- 이 주제는 형태 분석이나 문법 카드에 한정되지 않고 앱 전체의 정체성에 해당한다.
- 단어가 적을 때의 목록 UX와 수백·수천 개가 쌓였을 때의 관리 UX는 달라야 한다.
- 단어뿐 아니라 원문 문장, 분석 결과, 문법·표현, 복습 기록, 출처도 함께 쌓인다.
- 노트만으로 충분한지, 태그·책·챕터·출처 같은 보조 분류가 필요한지 관찰한다.
- 오래전에 충분히 익힌 단어는 삭제보다 보관 또는 정리 추천이 더 적합할 수 있다.
- 중복 단어가 다른 문장과 의미로 다시 들어왔을 때 합칠지 별도 항목으로 둘지 결정해야 한다.
- 검색, 필터, 일괄 이동, import/export가 데이터가 많아질수록 더 중요해진다.
- FSRS 복습 기록과 헷갈림 표시를 단어 정리 판단에 어떻게 활용할지 검토한다.

열린 질문:

- 같은 단어의 다른 뜻과 다른 예문은 한 카드 안에 쌓을까, 별도 카드로 둘까?
- 책과 챕터는 노트로 충분할까, 별도의 출처 구조가 필요할까?
- 충분히 익힌 단어는 보관함으로 옮길까, 복습 주기만 길게 둘까?
- 사용자가 직접 정리하게 할까, 앱이 정리 후보만 추천할까?

논의 기록:

- `2026-08-01`: 쌓이는 학습 데이터의 관리는 특정 기능의 부속 항목이 아니라 앱 전체를 관통하는 제품 정체성으로 정의했다.
- `2026-08-01`: 구체적인 정리 방식은 미리 확정하지 않고 실제 사용 경험을 기록하며 발전시키기로 했다.

## 현재 구현된 기반

- [x] 이메일 로그인, 회원가입, 로그인 유지
- [x] 일본어, 영어, 중국어 학습 언어 관리
- [x] 단어 직접 추가, 수정, 검색
- [x] AI 읽기·뜻 추천과 캐시
- [x] 노트 생성, 수정, 삭제
- [x] 여러 단어의 노트 이동과 삭제
- [x] OCR 사진 업로드, 텍스트 추출, 표현 staging, 저장
- [x] 일본어 세로쓰기 읽기 순서 보정
- [x] FSRS 기반 복습과 노트별 복습
- [x] 원어 기준, 뜻 기준 복습 방향 전환
- [x] 사용자별 기본 글자, 큰 글자 설정
- [x] 모바일 하단 탭과 데스크탑 좌측 내비게이션
